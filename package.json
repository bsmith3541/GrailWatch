{
  "name": "grail-watch",
  "version": "1.0.0",
  "description": "GrailWatch notifies Grailed users when new items that match their saved filters are added to Grailed's marketplace.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Brandon Smith",
  "license": "ISC",
  "dependencies": {
    "express": "^4.13.4",
    "http": "0.0.0",
    "mocha": "^2.4.5",
    "really-need": "^1.9.2",
    "request": "^2.69.0",
    "supertest": "^1.2.0"
  }
}
